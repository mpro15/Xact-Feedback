// filepath: c:\Users\Administrator\Documents\Xact-Feedback\src\pages\auth\LoginPage.tsx
import React, { useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { useNotification } from '../../contexts/NotificationContext';
import { LoadingSpinner } from '../../components/ui/LoadingSpinner';

export const LoginPage: React.FC = () => {
  const navigate = useNavigate();
  const { addNotification } = useNotification();
  
  // Auto-redirect to dashboard immediately
  useEffect(() => {
    console.log("AUTH DISABLED: Auto-redirecting to dashboard");
    
    addNotification({
      type: 'info',
      title: 'Authentication Disabled',
      message: 'Authentication bypassed. Redirecting to dashboard...'
    });
    
    // Brief delay to show the notification
    const timeout = setTimeout(() => {
      navigate('/dashboard');
    }, 1500);
    
    return () => clearTimeout(timeout);
  }, [navigate, addNotification]);
  
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-indigo-50 to-blue-100">
      <div className="max-w-md w-full px-6 py-8 bg-white rounded-xl shadow-lg">
        <div className="text-center mb-8">
          <h1 className="text-2xl font-bold text-gray-800 mb-2">Authentication Disabled</h1>
          <p className="text-gray-600">Redirecting to dashboard automatically...</p>
          <div className="mt-6">
            <LoadingSpinner size="large" />
          </div>
        </div>
      </div>
    </div>
  );
};
