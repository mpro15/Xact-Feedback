# Xact-Feedback

# Pending Tasks

## Profile Section
- Fix issue where uploading a photo vanishes on reload.

## Candidates Page
- Resolve errors in candidate upload functionality.
- Resolve errors in bulk candidate upload functionality.
- Copy the UI for the recent candidates section from the dashboard page to the candidates page.
- Ensure the status field is populated correctly.
- Ensure the "Send Feedback" button works.
- Fix issue where saving a candidate does not complete, even though the data is updated in the database.

## Dashboard Page
- Clicking the top-left logo should navigate the user to the home page (dashboard page).
- Clicking the cards should navigate the user to relevant pages.
- Ensure the Performance Overview chart shows real data (indicate 0 or no data if applicable).
- Ensure the buttons in the recent candidates pane are functional.

## View Candidate Modal
- Generate AI feedback using OpenAI API key instead of mock data.
- Ensure email preview, edit, and send buttons work.
- Replace mock analytics data with real data.
- Add actions to buttons in the "Generate Feedback" section.
- Remove settings from "Generate Feedback" or provide a link to the actual settings page.
- Fix the "Download PDF" functionality.

## Analytics Page
- Ensure all cards display unique tables instead of the same table.
- Improve the UI for rejection state distribution to be more graphic and visually appealing.
- Enable clicking on the rejection state chart to show candidates.
- Fix errors related to country code.
- Populate the rejection state field in the "Performance by Rejection State" chart.
- Ensure the filter works in the rejection state chart.

## Billing Page
- Ensure the "Top Up" button redirects to the payment gateway.
- Test credit usage functionality.
- Test the payment gateway.
- Test the payment gateway functionality.

## Settings Page
- Remove the first section (including SMTP settings).
- Remove email configuration settings.
- Fix issue where updating the company logo does not update the preview.
- Ensure the "Update All Settings" button works correctly in Behavior Controls, Privacy, and ATS Integration sections.
- Make the "Preview and Test" section similar to the candidate page's "Generate Feedback" interface.

## Header Section
- Fix the profile icon functionality in the top-right header section (determine if it is needed).
- Ensure the search bar in the header works.

## Onboarding Page
- Resolve issues preventing the onboarding page from functioning.

## Email Integration
- Implement email integration.

## Reports
- Fix issue where the generated PDF report seems dummy.

## API Keys
- Utilize OAI API key for integration.

## Notifications
- Implement a flow to send notifications that will be visible in the header section's notification button.

---
Feel free to update this list as tasks are completed or new ones are identified.